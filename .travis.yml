env:
  - DOCKER_COMPOSE_VERSION=1.4.2
os: 
  - linux
arch: 
  - arm64
  - amd64
dist: bionic
services: 
  - docker

before_install:
  - docker-compose -p $TRAVIS_COMMIT build -q

deploy:
  provider: script
  script: bash .travis/docker_push.sh
  on:
    branch: INFRA-243
